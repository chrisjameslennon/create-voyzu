{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    ".run/voyzu/packages/@voyzu/cli"
  ],
  "scripts": {
    "dev": "npm --prefix .run run dev",
    "build": "npm --prefix .run run build",
    "prestart": "npm run build",
    "start": "npm --prefix .run run start",
    "typecheck": "npm --prefix .run run typecheck"
  }
}
