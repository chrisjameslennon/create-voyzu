# Voyzu Modules Development Runtime

This disposable runtime links the parent `voyzu-modules` source into Voyzu.

Run development commands from the `voyzu-modules` repository root:

```shell
npm run dev
npm run build
npm run typecheck
```
